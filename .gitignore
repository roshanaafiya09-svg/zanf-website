node_modules
.next
out
.env
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Machine-local tool settings. launch.json hard-codes absolute paths to this
# machine's node binary, so it is useless — and misleading — to anyone else.
.claude/
